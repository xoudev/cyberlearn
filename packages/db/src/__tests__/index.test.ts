import { describe, expect, it } from "vitest";

describe("@cyberlearn/db", () => {
  it("package is importable", () => {
    // Placeholder test — real tests will be added in Phase 1 (BDD & Auth)
    expect(true).toBe(true);
  });
});
