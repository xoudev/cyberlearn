import { describe, expect, it } from "vitest";

describe("@cyberlearn/ui", () => {
  it("package is importable", () => {
    // Placeholder test — real component tests will be added in Phase 2 (Design System)
    expect(true).toBe(true);
  });
});
