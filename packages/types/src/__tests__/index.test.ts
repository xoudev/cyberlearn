import { describe, expect, it } from "vitest";

describe("@cyberlearn/types", () => {
  it("package is importable", () => {
    // Placeholder test — schema tests will be added as schemas are defined
    expect(true).toBe(true);
  });
});
