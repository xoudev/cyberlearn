# Public Supabase config for the mobile app (safe to expose - RLS protects data).
# Copy to .env.local and fill from the same project the web app uses.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
