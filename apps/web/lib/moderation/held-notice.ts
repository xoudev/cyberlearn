/**
 * What somebody is told when the screen takes their message down. Written once
 * in @cyberlearn/lib, where the app reads it too; re-exported here so the
 * site's forms keep their import.
 */
export { HELD_FOR_REVIEW } from "@cyberlearn/lib/moderation/notice";
